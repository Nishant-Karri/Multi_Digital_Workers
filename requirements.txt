# Multi_Digital_Workers — Python dependencies
# Install: pip install -r requirements.txt
# Minimum: Python 3.11+

# ── Core (always required) ─────────────────────────────────────────────────
requests>=2.31.0           # alerts (Teams, Slack), JIRA REST, Informatica, Talend

# ── Snowflake ─────────────────────────────────────────────────────────────
snowflake-connector-python>=3.6.0
snowflake-sqlalchemy>=1.5.0   # optional — for SQLAlchemy ORM support

# ── AWS ───────────────────────────────────────────────────────────────────
boto3>=1.34.0              # S3, Glue, Kinesis, SageMaker, Secrets Manager

# ── Azure / KQL ───────────────────────────────────────────────────────────
azure-kusto-data>=4.3.0    # Azure Data Explorer / KQL
azure-identity>=1.15.0     # Azure AD auth (KQL, Graph API, Azure ML)
azure-ai-ml>=1.13.0        # Azure ML (optional — for mlops domain)

# ── Data processing ────────────────────────────────────────────────────────
pandas>=2.1.0
pyarrow>=14.0.0            # Parquet read/write

# ── Encryption (vault local backend) ──────────────────────────────────────
cryptography>=41.0.0       # Fernet encryption for .vault/secrets.enc

# ── dbt ───────────────────────────────────────────────────────────────────
# Uncomment the adapter(s) you use:
# dbt-snowflake>=1.7.0
# dbt-bigquery>=1.7.0
# dbt-redshift>=1.7.0
# dbt-databricks>=1.7.0

# ── Streaming (optional) ──────────────────────────────────────────────────
# confluent-kafka>=2.3.0   # Kafka consumer/producer
# google-cloud-pubsub>=2.19.0  # GCP Pub/Sub

# ── ML / feature store (optional) ─────────────────────────────────────────
# feast>=0.35.0            # Feature store
# mlflow>=2.10.0           # ML experiment tracking
# scikit-learn>=1.4.0      # ML training
# xgboost>=2.0.0

# ── Data quality (optional) ────────────────────────────────────────────────
# great-expectations>=0.18.0

# ── Data catalog (optional) ────────────────────────────────────────────────
# acryl-datahub>=0.12.0   # DataHub lineage emitter

# ── Database connectors (optional) ────────────────────────────────────────
# psycopg2-binary>=2.9.9  # PostgreSQL / Redshift
# mysql-connector-python>=8.3.0
# pyodbc>=5.0.0            # SQL Server

# ── GCP (optional) ────────────────────────────────────────────────────────
# google-cloud-bigquery>=3.14.0
# google-auth>=2.27.0

# ── Databricks (optional) ─────────────────────────────────────────────────
# databricks-sdk>=0.20.0
# databricks-sql-connector>=3.0.0

# ── Dev / testing ──────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0
ruff>=0.2.0                # linter (used in CI)
